name: Deploy Web to Cloudflare Pages

on:
  push:
    branches:
      - main
    paths:
      - 'apps/web/**'
      - 'packages/core/**'
      - 'packages/sdk/**'
      - '.github/workflows/deploy-web.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'apps/web/**'
      - 'packages/core/**'
      - 'packages/sdk/**'
  workflow_dispatch:

# Cancel in-progress deployments for the same PR/branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '9.15.4'

jobs:
  lint-and-test:
    name: Lint & Test Web App
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Build workspace deps so @creepjs/core types resolve for lint/typecheck
      - name: Build workspace packages
        run: |
          pnpm --filter @creepjs/core build
          pnpm --filter @creepjs/sdk build

      - name: Lint web code
        run: pnpm --filter @creepjs/web lint

      - name: Type check
        run: pnpm --filter @creepjs/web typecheck

      - name: Run unit tests
        run: pnpm --filter @creepjs/web test
        env:
          NODE_ENV: test

  build:
    name: Build Next.js Application
    needs: lint-and-test
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Setup Next.js cache
        uses: actions/cache@v3
        with:
          path: |
            apps/web/.next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ hashFiles('apps/web/**/*.js', 'apps/web/**/*.jsx', 'apps/web/**/*.ts', 'apps/web/**/*.tsx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: |
          pnpm --filter @creepjs/core build
          pnpm --filter @creepjs/sdk build

      - name: Stage SDK browser bundles
        run: |
          cp packages/sdk/dist/creepjs.umd.js apps/web/public/sdk.js
          cp packages/sdk/dist/creepjs.full.umd.js apps/web/public/sdk.full.js

      - name: Build Next.js app
        run: pnpm --filter @creepjs/web build
        env:
          NEXT_PUBLIC_API_URL: https://api.creepjs.org
          NEXT_PUBLIC_SITE_URL: https://creepjs.org
          NEXT_PUBLIC_ENV: production

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nextjs-build
          path: apps/web/out
          retention-days: 1

  deploy-production:
    name: Deploy to Cloudflare Pages (Production)
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    # Only deploy on push to main or manual workflow dispatch
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'

    environment:
      name: production
      url: https://creepjs.org

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # wrangler-action needs pnpm available to install/run wrangler.
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: nextjs-build
          # Artifact holds the CONTENTS of out/, so restore it INTO apps/web/out
          # to match `directory: apps/web/out` below.
          path: apps/web/out

      # Deploy via standalone wrangler (npx) — this job has no pnpm install, so
      # wrangler-action's package-manager path fails. npx fetches wrangler directly.
      - name: Deploy to Cloudflare Pages
        run: npx --yes wrangler@3 pages deploy apps/web/out --project-name=creepjs --branch=main
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Wait for deployment to propagate
        run: sleep 15

      - name: Health check - Homepage
        id: health-check-home
        run: |
          echo "Testing homepage..."
          response=$(curl -s -o /dev/null -w "%{http_code}" https://creepjs.org/ || echo "000")
          echo "Response code: $response"

          if [ "$response" = "200" ]; then
            echo "✅ Homepage check passed!"
          else
            # Non-fatal: the wrangler deploy step is authoritative. Cloudflare edge
            # security (Bot Fight / WAF) may 403 the runner's datacenter IP.
            echo "::warning::Post-deploy edge probe returned $response (likely Cloudflare bot/WAF on the runner IP), not failing the deploy."
          fi
          echo "status=success" >> $GITHUB_OUTPUT

      - name: Health check - Checker page
        if: steps.health-check-home.outputs.status == 'success'
        run: |
          echo "Testing checker page..."
          response=$(curl -s -o /dev/null -w "%{http_code}" https://creepjs.org/checker || echo "000")
          echo "Response code: $response"

          if [ "$response" = "200" ]; then
            echo "✅ Checker page check passed!"
          else
            echo "⚠️  Checker page returned: $response"
          fi

      - name: Health check - Sitemap
        if: steps.health-check-home.outputs.status == 'success'
        run: |
          echo "Testing sitemap..."
          response=$(curl -s -o /dev/null -w "%{http_code}" https://creepjs.org/sitemap.xml || echo "000")
          echo "Response code: $response"

          if [ "$response" = "200" ]; then
            echo "✅ Sitemap check passed!"
          else
            echo "⚠️  Sitemap returned: $response (may not exist yet)"
          fi

      - name: Test Core Web Vitals
        if: steps.health-check-home.outputs.status == 'success'
        run: |
          echo "📊 Core Web Vitals will be monitored via Cloudflare Analytics"
          echo "Target metrics: LCP < 2.5s, FID < 100ms, CLS < 0.1"

      - name: Report deployment success
        if: success()
        run: |
          echo "::notice::✅ Web app deployed successfully to https://creepjs.org"
          echo "::notice::🎯 Check Cloudflare Pages dashboard for deployment details"

      - name: Notify on failure
        if: failure()
        run: |
          echo "::error::Web deployment failed. Check logs for details."

  deploy-preview:
    name: Deploy PR Preview to Cloudflare Pages
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: nextjs-build
          path: apps/web

      - name: Deploy preview to Cloudflare Pages
        id: deploy-preview
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: creepjs
          directory: apps/web/out
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.head_ref }}

      - name: Wait for preview deployment
        run: sleep 10

      - name: Comment PR with preview URL
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            const pr_number = context.payload.pull_request.number;
            const preview_url = `https://${{ github.head_ref }}.creepjs.pages.dev`;

            github.rest.issues.createComment({
              issue_number: pr_number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🚀 **Web Preview Deployed**\n\n✨ Preview URL: ${preview_url}\n\n📊 Test the changes:\n- Homepage: ${preview_url}\n- Checker: ${preview_url}/checker\n- Docs: ${preview_url}/docs\n- Playground: ${preview_url}/playground\n\n⏱️  Check [Cloudflare Pages dashboard](https://dash.cloudflare.com) for detailed metrics.`
            });

  lighthouse:
    name: Lighthouse Performance Check
    needs: deploy-preview
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v10
        with:
          urls: |
            https://${{ github.head_ref }}.creepjs.pages.dev
            https://${{ github.head_ref }}.creepjs.pages.dev/checker
            https://${{ github.head_ref }}.creepjs.pages.dev/guide/what-is-browser-fingerprinting
            https://${{ github.head_ref }}.creepjs.pages.dev/fingerprint/canvas
          configPath: '.github/lighthouserc.json'
          uploadArtifacts: true
          temporaryPublicStorage: true

      - name: Comment Lighthouse results
        uses: actions/github-script@v7
        if: always()
        with:
          script: |
            const fs = require('fs');
            const pr_number = context.payload.pull_request.number;

            let comment = '📊 **Lighthouse Performance Report**\n\n';
            comment += 'Performance audit completed. Check artifacts for detailed results.\n';
            comment += '\n🎯 Target: Performance > 90, Accessibility > 95, Best Practices > 90, SEO > 90';

            github.rest.issues.createComment({
              issue_number: pr_number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
